name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:      
      - uses: actions/checkout@v3
      - name: Run tex2html
        run: ./tex2html.py [main.tex] [kevin-loo.github.io/HMQFT.html]
